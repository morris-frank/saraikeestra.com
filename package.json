{
  "name": "saraikeestra-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "preview": "pnpm build && node scripts/preview.mjs",
    "check": "pnpm build"
  },
  "engines": {
    "node": ">=20"
  }
}
